xpBrainDuino is an Arduino program that communicates with the X-Plane rduino
Broker Plugin.

ArduinoBroker is an X-Plane plugin that communicates with an Arduino that is
running the xpBrainDuino program.

